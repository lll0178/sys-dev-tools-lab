[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "greetlab-25030021043"      # ← 这里！把"学号"换成你的真实学号
version = "0.1.0"

[project.scripts]
sdt-greet = "greetlab.cli:main"
